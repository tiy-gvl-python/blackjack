class CardsAndDecks():

# define card constants (FACE_VALUES, SUITS)
# create deck with list comprehension
# deck = ["{0} of {1}".format(f, s) for f in FACE for s in SUITS]
# shuflle the deck with shuffle(deck)
# ask if game is new (which is a boolean) if game new is True shuflle deck
# return hands
# shuffled deck will be assigned new variable = deck_cache
# hands are taken from deck_cache and each card taken is subtracted from the deck
# if a dealer, or player hits, then the card given is also subtracted from deck_cache

# from random import shuffle (to shuffle the deck)
# define point values
