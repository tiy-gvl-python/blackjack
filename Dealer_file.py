import Deck_file
from Deck_file import Deck
import Card_file
from Card_file import Card


class Dealer():

    def dealer_run(self):
        # if self.check_16(hand):
            # pass
        hitme("dealer")
        new_card.count(hand, "dealer")

    """def check_16(self):
        if check_hand(hand) == 16:
            return False"""
